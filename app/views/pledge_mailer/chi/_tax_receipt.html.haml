%p
  %pre
    = preserve do
      :escaped
        DONATION RECEIPT FOR YOUR TAX RECORDS
        Name:        #{@donor.first_name.capitalize} #{@donor.last_name.capitalize}
        Campaign:    #{@campaign.name} #{@campaign.campaign_type.capitalize} Match
        Amount:      #{number_to_currency(@donor.donation_amount)}
        Date:        #{@donor.processed_at.strftime('%m/%d/%Y')}
        Credit card: XXXX-XXXX-XXXX-#{@stripe_customer.active_card.last4}

